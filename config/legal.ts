/**
 * Version du texte des CGV actuellement en ligne (app/cgv/page.tsx).
 *
 * À incrémenter à CHAQUE modification de fond des conditions générales :
 * chaque consentement est enregistré avec cette valeur, de sorte qu'on
 * puisse prouver quel texte exact un client a accepté et à quelle date.
 */
export const VERSION_CGV = "cgv-2026-07-30";

/** Taux de commission par défaut appliqué aux nouveaux dossiers. */
export const TAUX_COMMISSION = 0.22;
